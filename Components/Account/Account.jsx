import { Title } from '../Globals/Title/Title';

export const Account = () => {
  return (
    <section>
      <header>
        <Title>My Account</Title>
        <nav></nav>
      </header>
    </section>
  );
};
